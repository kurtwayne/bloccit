<h1>Edit Advertisements</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for advertisements<p>
      <ul>
        <li>Make sure it ryhmes.</li>
        <li>Don't us the letter "A".</li>
        <li>The incessant use of hashtags will get you banned.</li>
      </ul>
    </div>
    <div class="col-md-8">
      <%=form_for @advertisement do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form_control', placeholder: "Enter advertisement title" %>
      </div>
      <div class="form-group">
        <%= f.label :copy %>
        <%= f.text_area :copy, rows: 8, class: 'form_control' , placeholder: "Enter advertisement copy" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
      <% end %>
    </div>
  </div>
